import CheckPresenceForm from "../components/meetingPresence/components/CheckPresenceForm";

export default function CheckPresencePage() {
  return (
    <div className="flex h-screen w-full items-center justify-center">
      <CheckPresenceForm />
    </div>
  );
}
