export type ContactResponse = {
  _id: string;
  name: string;
  phoneNumber: string;
  description: string;
  createdAt: string;
};

export type Contact = {
  id: string;
  name: string;
  phoneNumber: string;
  description: string;
};
